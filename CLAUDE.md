@AGENTS.md

# Claude Code Specific Notes

- This is the hub repo, not the pipeline. Pipeline code lives in
  `project-template`; the LaTeX paper lives in `overleaf-template`
  (both in `c:/_git/`).
- When the user asks to change the pipeline, switch into
  `project-template` rather than recreating `src/` here.
- Do not create new files unless necessary. Prefer editing existing files.
