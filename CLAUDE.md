@AGENTS.md

# Claude Code Specific Notes

- This is the hub repo, not the pipeline. Code lives in `project-template-r`,
  `project-template`, and `overleaf-template` (all in `c:/_git/`).
- When the user asks to change the pipeline, ask which template the change
  belongs in rather than recreating `src/` here.
- Do not create new files unless necessary. Prefer editing existing files.
